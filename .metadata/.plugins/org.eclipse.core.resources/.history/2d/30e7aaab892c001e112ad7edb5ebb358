package entities;

public class Triangulo {
	public double a,b,c,area;
	
	public double calculaArea() {
		double p,area;
		
		p= (a+b+c)/2;
		area = Math.sqrt(p * (p-a)*(p-b)*(p-c));
		return area;
	}
}
